{
  "name": "${name}",
  "version": "${version}",
  "description": "${description}",
  "java": {
    "version": "${targetCompatibility}"
  },
  "flink": {
    "version": "${flinkVersion}"
  },
  "nodes" : [{
    "name": "Anchor",
    "type": "transform",
    "icon": "tree-file",
    "classname": "com.iisquare.fs.flink.plugins.olap.node.AnchorNode",
    "property": [],
    "returns": []
  }]
}