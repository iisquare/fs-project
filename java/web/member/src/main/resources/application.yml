spring:
  profiles:
    active: dev,idev
  jpa:
    show-sql: true
    hibernate:
      naming:
        physical-strategy: com.iisquare.fs.web.member.dsconfig.NamingStrategy

server:
  servlet:
    timeout: 86400
    session:
      cookie:
        max-age: 2592000
