
subprojects {
    apply plugin: 'org.springframework.boot'

    archivesBaseName = rootProject.name + '-site-' + name

    dependencies {
        compile project(':base:web')
    }

    bootJar {
        enabled = true
    }

    jar {
        enabled = true
    }

}
