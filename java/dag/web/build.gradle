apply plugin: 'org.springframework.boot'

dependencies {
    compile project(':dag:core')
    compile project(':web:core')
    compile project(':base:jpa')
    compile project(':base:swagger')
}

bootJar {
    enabled = true
}

jar {
    enabled = true
}
