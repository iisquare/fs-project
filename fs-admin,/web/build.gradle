
subprojects {
    apply plugin: 'org.springframework.boot'

    archivesBaseName = rootProject.name + '-web-' + name

    bootJar {
        enabled = true
    }

    jar {
        enabled = true
    }

}
