spring:
  application:
    name: fs-member-service
  profiles:
    active: dev,idev
  jpa:
    show-sql: true
    hibernate:
      naming:
        physical-strategy: com.iisquare.fs.web.member.dsconfig.NamingStrategy

server:
  servlet:
    session:
      timeout: 30d
      cookie:
        max-age: 30d
