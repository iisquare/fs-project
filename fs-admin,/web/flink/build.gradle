
dependencies {
    implementation project(':web:core')
    implementation (project(':app:flink'))
    implementation group: 'org.apache.flink', name: 'flink-runtime-web', version: flinkVersion
    testImplementation project(':web:test')
}

